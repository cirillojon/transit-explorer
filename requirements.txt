# Runtime dependencies — PINNED for reproducible builds.
#
# Bump deliberately. To regenerate against a fresh upper-bound resolve:
#   pip install -U -r requirements-dev.txt   # or your unpinned spec
#   pip freeze | grep -iE '^(flask|werkzeug|gunicorn|sqlalchemy|...)='
#
# Keep this list to direct (top-level) deps only; transitive ones are
# pulled in automatically. If you need fully reproducible installs
# including transitives, layer a `pip-compile`d requirements.lock on top.
Flask==3.1.3
Werkzeug==3.1.8
gunicorn==23.0.0
SQLAlchemy==2.0.49
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Cors==5.0.1
Flask-Limiter==3.12
requests==2.32.3
httpx==0.28.1
psutil==5.9.0
onebusaway==1.24.1
firebase-admin==6.9.0
python-dotenv==1.0.1
python-json-logger==2.0.7
sentry-sdk[flask]==2.58.0